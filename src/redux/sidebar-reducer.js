let initialState = {
            friends : [
                {id: 1, friend: 'Sasha'},
                {id: 2, friend: 'Boris'},
                {id: 3, friend: 'Pavel'}
            ]
    }
    

const sidebarReducer = (state = initialState, action) => {
    return state;
}

export default sidebarReducer;